# 정책단가 조회 프로그램 — 인터넷에 올려서 링크로 쓰기

지금까지는 내 PC를 켜놔야만 접속되는 방식이었습니다. 아래 방법을 따라 하면
**언제든 접속 가능한 고정 링크**(예: `https://내앱이름.streamlit.app`)가 생깁니다.
Streamlit Community Cloud라는 **무료 호스팅 서비스**를 사용합니다.

⚠️ 무료 버전은 코드 저장소를 **공개(Public)**로 만들어야 합니다.
`app.py`, `requirements.txt` 코드 자체는 공개되지만(가격 계산 로직 등),
아래 방법을 따르면 실제 **정책 데이터(policy.db)와 API 키는 공개되지 않습니다.**

---

## 1단계 — GitHub 계정 만들기
1. https://github.com 접속 → 오른쪽 위 **Sign up**
2. 이메일/비밀번호로 가입 (무료)

## 2단계 — 새 저장소(Repository) 만들기
1. 로그인 후 오른쪽 위 **+** 버튼 → **New repository**
2. Repository name: `policy-price-checker` 등 원하는 이름
3. **Public** 선택 (무료 배포는 Public만 가능)
4. **Create repository** 클릭

## 3단계 — 파일 업로드
1. 방금 만든 저장소 페이지에서 **Add file → Upload files** 클릭
2. `app.py`, `requirements.txt`, `.gitignore` 파일을 끌어다 놓기
   (`policy.db`, `api_key.txt`는 **올리지 마세요** — 개인 정보/정책 데이터이므로 제외)
3. 아래 **Commit changes** 클릭

## 4단계 — Streamlit Community Cloud에서 배포
1. https://share.streamlit.io 접속
2. **Sign in with GitHub**로 로그인 (방금 만든 계정으로)
3. **Create app** (또는 **New app**) 클릭
4. **Deploy a public app from GitHub** 선택
5. Repository: 방금 만든 `policy-price-checker` 선택
6. Main file path: `app.py` 입력
7. **Deploy** 클릭 → 1~3분 정도 기다리면 배포 완료

배포가 끝나면 `https://XXXX.streamlit.app` 같은 주소가 생깁니다.
이 주소가 바로 휴대폰에서든 어디서든 접속 가능한 **정책단가 조회 링크**입니다.

## 5단계 — API 키를 안전하게 등록하기 (Secrets)
API 키를 코드에 직접 넣거나 GitHub에 올리면 안 됩니다. 대신 Streamlit이
제공하는 **Secrets(비밀 저장소)** 기능을 사용합니다.

1. 배포된 앱 페이지 오른쪽 아래(또는 우측 상단) **⋮ (점 세개)** 메뉴 → **Settings**
2. **Secrets** 탭 클릭
3. 아래처럼 입력:
   ```
   GEMINI_API_KEY = "여기에_복사한_AIza로_시작하는_키_붙여넣기"
   ```
4. **Save** 클릭 → 앱이 자동으로 재시작됩니다.

이렇게 등록하면 앱의 "구글 Gemini API 키" 칸을 채우지 않아도 자동으로 이 키를 사용합니다.

---

## ⚠️ 알아두어야 할 점
- **무료 버전은 저장소가 공개(Public)**입니다. 코드는 보이지만, `.gitignore`
  덕분에 실제 정책 단가 데이터(`policy.db`)와 API 키는 올라가지 않습니다.
- 무료 호스팅은 앱을 오래 쓰지 않으면 "잠자기" 상태가 되고, 다시 켜질 때
  **정책 데이터가 초기화(샘플 데이터로 리셋)될 수 있습니다.** 회사 내부용으로
  중요한 데이터라면, 나중에는 더 안정적인 저장 방식(구글 시트 연동, 유료 DB 등)으로
  업그레이드하는 걸 권장합니다. 지금은 우선 "링크로 접속되는 MVP"를 만드는 단계입니다.
- 회사 보안 규정상 외부 클라우드 서비스 사용이 제한될 수 있으니, 사내 배포가
  필요하다면 IT 담당자와 상의하는 것도 고려해보세요.
